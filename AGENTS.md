# AGENTS

- Основной интерфейс для разработки — версия Rust в каталоге `rust/ui`. Все UX-изменения и новые функции следует реализовывать там; обновление старого `frontend/` допустимо только при прямом запросе.
