# Руководство по использованию Live Транскрипции (Streaming)

**Дата:** 11 декабря 2025  
**Версия:** 1.0  
**Статус:** ✅ Production Ready

## Обзор

Live Транскрипция (Streaming Transcription) — это функция real-time транскрипции речи во время записи с минимальной задержкой (<500ms). Использует NVIDIA Parakeet TDT v3 (0.6B) через FluidAudio StreamingAsrManager.

## Возможности

- ⚡ **Real-time обновления** — текст появляется во время речи
- 🎯 **Volatile vs Confirmed** — промежуточные гипотезы (серый текст) и подтверждённый текст (чёрный)
- 📊 **Индикатор уверенности** — визуальная индикация confidence модели
- 🔄 **Автоскролл** — автоматическая прокрутка к новому тексту
- 🎨 **Плавные анимации** — smooth transitions между состояниями

## Требования

- **Модель:** Parakeet TDT v3 (0.6B) — скачивается автоматически (~640 MB)
- **macOS:** 14.0+ (для FluidAudio)
- **Память:** ~2 GB RAM
- **Первый запуск:** ~35 секунд для загрузки модели

## Как использовать

### 1. Включение в настройках

1. Откройте настройки (кнопка ⚙️ в header)
2. Найдите чекбокс **"Live Транскрипция"** с меткой `Beta`
3. Включите чекбокс
4. Настройка сохраняется автоматически в localStorage

### 2. Запуск записи

1. Выберите модель Parakeet TDT v3 в Model Manager
2. Нажмите кнопку **"Start Recording"**
3. Streaming автоматически активируется (если включен в настройках)

### 3. Просмотр Live транскрипции

1. Во время записи появится кнопка **"Live"** в RecordingOverlay
2. Нажмите на неё для открытия панели Live транскрипции
3. Панель показывает:
   - **Volatile text** (серый, курсив) — промежуточные гипотезы
   - **Confirmed text** (чёрный, нормальный) — подтверждённый текст
   - **Confidence indicator** — цветной индикатор уверенности модели:
     - 🟢 Зелёный: ≥85% (высокая)
     - 🟡 Жёлтый: 70-84% (средняя)
     - 🔴 Красный: <70% (низкая)
   - **Live indicator** — пульсирующий индикатор активности

### 4. Остановка записи

1. Нажмите **"Остановить"** в RecordingOverlay
2. Streaming автоматически завершается
3. Финальный текст сохраняется в сессию

## Технические детали

### Архитектура

```
Frontend (React)
  ↕ WebSocket
Backend Server (Go)
  ↕ Callback
StreamingTranscriptionService (Go)
  ↕ Line-delimited JSON
transcription-fluid-stream (Swift)
  ↕ FluidAudio API
StreamingAsrManager → Apple Neural Engine
```

### Параметры

- **chunk_seconds:** 15s (основной размер чанка)
- **confirmation_threshold:** 0.85 (порог для confirmed text)
- **left_context:** 10s (улучшает точность на границах)
- **right_context:** 2s (lookahead, добавляет latency)

### Производительность

- **Latency (p95):** < 500ms
- **RTFx:** > 100x (Real-time Factor)
- **WER:** 1.93% (Word Error Rate)
- **First update:** ~2-3s (после накопления минимального контекста)
- **Memory:** ~2 GB

### Протокол WebSocket

#### Frontend → Backend

```json
// Включить streaming
{"type": "enable_streaming"}

// Выключить streaming
{"type": "disable_streaming"}

// Получить статус
{"type": "get_streaming_status"}
```

#### Backend → Frontend

```json
// Обновление транскрипции
{
  "type": "streaming_update",
  "streamingText": "Hello world",
  "streamingIsConfirmed": true,
  "streamingConfidence": 0.95,
  "streamingTimestamp": 1702345678901
}

// Статус
{"type": "streaming_enabled"}
{"type": "streaming_disabled"}
{"type": "streaming_status", "data": "true"}
```

## Компоненты

### Frontend

- **`StreamingTranscription.tsx`** — компонент отображения
- **`useStreamingTranscription.ts`** — hook для state management
- **`RecordingOverlay.tsx`** — интеграция кнопки "Live"
- **`SettingsPanel.tsx`** — чекбокс настроек

### Backend

- **`streaming_transcription.go`** — сервис управления streaming
- **`transcription-fluid-stream`** — Swift CLI для FluidAudio
- **`server.go`** — WebSocket обработчики
- **`recording.go`** — передача аудио в streaming service

## Troubleshooting

### Модель не загружается

**Проблема:** "Failed to enable streaming transcription"

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь что есть ~1 GB свободного места
3. Проверьте логи: `tail -f ~/Library/Logs/AIWisper/backend.log`
4. Попробуйте перезапустить приложение

### Нет обновлений во время записи

**Проблема:** Панель Live открыта, но текст не появляется

**Решение:**
1. Убедитесь что чекбокс "Live Транскрипция" включен в настройках
2. Проверьте что выбрана модель Parakeet TDT v3
3. Говорите громче — модель требует чёткую речь
4. Проверьте логи backend на ошибки

### Высокая задержка (>1s)

**Проблема:** Текст появляется с большой задержкой

**Решение:**
1. Закройте другие приложения (освободите RAM)
2. Проверьте загрузку CPU (Activity Monitor)
3. Убедитесь что используется Apple Neural Engine (не CPU fallback)

### Низкая точность

**Проблема:** Много ошибок в транскрипции

**Решение:**
1. Говорите чётче и медленнее
2. Уменьшите фоновый шум
3. Проверьте качество микрофона
4. Используйте batch транскрипцию для финальной версии

## Ограничения

1. **Только микрофон** — streaming работает только с микрофонным каналом (моно)
2. **Без диаризации** — нет разделения спикеров в real-time
3. **Английский язык** — Parakeet TDT v3 оптимизирован для английского
4. **macOS only** — требует Apple Neural Engine для производительности

## Roadmap

### Ближайшие улучшения

- [ ] **Hypothesis chunks** — обновления каждые 1s (сейчас 15s)
- [ ] **Streaming + Diarization** — real-time speaker labels
- [ ] **Multi-language** — поддержка других языков
- [ ] **Export** — сохранение streaming transcript в SRT/VTT

### Долгосрочные планы

- [ ] **Metrics dashboard** — графики latency, RTFx, confidence
- [ ] **Custom models** — поддержка других streaming моделей
- [ ] **Cloud streaming** — опциональный cloud inference

## FAQ

**Q: Можно ли использовать streaming с другими моделями?**  
A: Нет, сейчас поддерживается только Parakeet TDT v3. Другие модели (Whisper, GigaAM) работают только в batch режиме.

**Q: Влияет ли streaming на качество финальной транскрипции?**  
A: Нет, streaming и batch транскрипция независимы. Финальная транскрипция выполняется отдельно после записи.

**Q: Можно ли использовать streaming без интернета?**  
A: Да, после первой загрузки модель кэшируется локально. Интернет нужен только для первого запуска.

**Q: Почему первый запуск долгий?**  
A: Модель Parakeet TDT v3 (~640 MB) загружается с сервера FluidAudio. Последующие запуски мгновенные.

**Q: Можно ли отключить streaming во время записи?**  
A: Да, просто закройте панель Live или выключите чекбокс в настройках перед следующей записью.

## Поддержка

- **GitHub Issues:** https://github.com/yourusername/aiwisper/issues
- **Документация:** `/docs/architecture_streaming_parakeet_2025-12-11.md`
- **Логи:** `~/Library/Logs/AIWisper/backend.log`

---

**Версия документа:** 1.0  
**Последнее обновление:** 11 декабря 2025
