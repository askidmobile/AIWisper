# Рефакторинг UI активного окна записи

**Дата:** 28 января 2025  
**Статус:** Завершено  
**Автор:** AI Assistant

## Цель

Упростить и унифицировать интерфейс активного окна записи, убрав модальное окно "Live" и показывая транскрипцию в едином формате с просмотром записей.

## Основные изменения

### 1. Создан компонент SystemStatus

**Файл:** `frontend/src/components/session/SystemStatus.tsx`

**Назначение:** Отображает текущую стадию обработки системы одной строкой с иконкой и индикатором.

**Стадии обработки (по приоритету):**
1. 🤖 Обработка LLM
2. ⚡ Гибридная обработка
3. 👥 Анализ спикеров
4. 🎤 Распознавание речи
5. ✓ Подтверждение текста
6. 📝 Стриминг транскрипция
7. 📦 Создание чанка
8. 🎙 Идёт запись аудио
9. ⏸ Ожидание аудио

**Особенности:**
- Подписывается на WebSocket события для определения текущей стадии
- Автоматически переключается между стадиями по приоритету
- Анимированный индикатор активности
- Цветовая кодировка для разных стадий

### 2. Создан компонент LiveChunksView

**Файл:** `frontend/src/components/chunks/LiveChunksView.tsx`

**Назначение:** Отображает чанки в реальном времени во время записи с интеграцией streaming транскрипции.

**Функционал:**
- Список чанков с автопрокруткой к последнему
- Интеграция StreamingTranscription в последний транскрибируемый чанк
- Анимация появления новых чанков (fade-in + slide-down)
- Пульсация транскрибируемого чанка
- Highlight эффект подтверждённого чанка (2 секунды)
- Индикатор "Новых чанков" при ручном скролле
- Кнопка "Скролл вниз" с счётчиком новых чанков

**Структура чанка:**
- Заголовок: номер, длительность, статус
- Кнопка воспроизведения (для завершённых чанков)
- Диалог с таймстемпами и спикерами
- Streaming область (для последнего чанка)
- Индикатор транскрипции

### 3. Рефакторинг StreamingTranscription

**Файл:** `frontend/src/components/StreamingTranscription.tsx`

**Изменения:**
- Добавлен prop `compact: boolean` для компактного режима
- В компактном режиме:
  - Убран header с "Live Транскрипция"
  - Убран border и background
  - Убран footer с легендой
  - Оставлен только текст + мини-индикатор confidence
- Полный режим остался без изменений (для обратной совместимости)

### 4. Рефакторинг RecordingOverlay

**Файл:** `frontend/src/components/RecordingOverlay.tsx`

**Удалено:**
- Кнопка "Live" с иконкой чата
- Модальное окно с Live транскрипцией
- Состояние `showStreaming`
- Анимация slideDown для модального окна

**Добавлено:**
- Компонент `<SystemStatus />` между таймером и кнопкой "Остановить"

**Новый layout:**
```
[🔴 Запись] [Волна ▂▄▆█] [11:17] [📝 Стриминг транскрипция...] [⏹ Остановить]
```

### 5. Рефакторинг RecordingView

**Файл:** `frontend/src/components/views/RecordingView.tsx`

**Удалено:**
- Двухколоночный layout (Pipeline Status + Live Transcription)
- Блок "Стадии обработки" с 5 этапами
- Компонент `StreamingTranscription` в полном режиме

**Добавлено:**
- Компонент `<LiveChunksView />` на всю ширину
- Подписка на события `chunk_transcribed` и `chunk_created`
- Управление состояниями `highlightedChunkId` и `transcribingChunkId`
- Функция `getSpeakerDisplayName` для цветовой кодировки спикеров

**Новый layout:**
```
┌─────────────────────────────────────────────────────┐
│ 🔴 Идёт запись — транскрипция в реальном времени   │
│ Чанки появляются автоматически по мере обработки    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ Чанк #0 ✓ 3.5s                          ▶  │   │
│  │ [00:00.0] Спикер 1: Привет!                │   │
│  │ [00:01.5] Спикер 2: Как дела?              │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ Чанк #1 ⏳ 2.1s                          ▶  │   │
│  │ [00:05.0] Спикер 1: Отлично                │   │
│  │                                             │   │
│  │ ┌─────────────────────────────────────────┐ │   │
│  │ │ А что у тебя нового... 85%              │ │   │
│  │ └─────────────────────────────────────────┘ │   │
│  │ ⏳ Транскрибируется...                      │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## Логика работы

### Сценарий 1: Начало записи

1. Пользователь нажимает "Начать запись"
2. Появляется `RecordingOverlay` сверху
3. `SystemStatus` показывает "🎙 Идёт запись аудио..."
4. `RecordingView` показывает "Ожидание первого чанка..."

### Сценарий 2: Появление streaming текста

1. Backend отправляет `streaming_update` событие
2. `SystemStatus` → "📝 Стриминг транскрипция..."
3. `LiveChunksView` создаёт первый чанк (если ещё нет)
4. Внутри чанка появляется **серый курсивный** streaming текст
5. Confidence индикатор обновляется в реальном времени

### Сценарий 3: Подтверждение текста

1. Backend отправляет `streaming_update` с `isConfirmed: true`
2. `SystemStatus` → "✓ Подтверждение текста..."
3. Streaming текст превращается в **обычный диалог** с таймстемпами
4. Чанк получает **highlight эффект** (зелёная рамка, пульсация)
5. Через 2 секунды highlight исчезает
6. `SystemStatus` возвращается к базовой стадии

### Сценарий 4: Создание нового чанка

1. Backend отправляет `chunk_created` событие
2. `SystemStatus` → "📦 Создание чанка..."
3. `LiveChunksView` добавляет новый чанк в список
4. **Автопрокрутка** к новому чанку (если пользователь внизу)
5. Streaming начинается в новом чанке

### Сценарий 5: Ручной скролл

1. Пользователь скроллит вверх для просмотра старых чанков
2. Автопрокрутка отключается
3. Появляется новый чанк
4. Показывается кнопка "Новых чанков: 1 ↓"
5. При клике на кнопку — скролл вниз и сброс счётчика

## Структура файлов

```
frontend/src/components/
├── chunks/
│   ├── ChunksView.tsx                    # Существующий (без изменений)
│   ├── ChunksViewSimple.tsx              # Существующий (без изменений)
│   ├── LiveChunksView.tsx                # НОВЫЙ
│   └── index.ts                          # Обновлён (добавлен экспорт)
│
├── session/
│   ├── SessionHeader.tsx                 # Существующий (без изменений)
│   ├── SystemStatus.tsx                  # НОВЫЙ
│   └── index.ts                          # Обновлён (добавлен экспорт)
│
├── views/
│   └── RecordingView.tsx                 # ИЗМЕНЁН (полный рефакторинг)
│
├── RecordingOverlay.tsx                  # ИЗМЕНЁН (убрана Live кнопка)
└── StreamingTranscription.tsx            # ИЗМЕНЁН (добавлен compact режим)
```

## Преимущества нового подхода

### 1. Единообразие UI
- Одинаковый формат отображения чанков при записи и просмотре
- Пользователь видит знакомый интерфейс
- Легче ориентироваться в данных

### 2. Удобство использования
- Не нужно открывать модальное окно для просмотра транскрипции
- Вся информация на одном экране
- Автопрокрутка с возможностью отключения

### 3. Информативность
- Видна вся история чанков, а не только текущий текст
- Статус каждого чанка (транскрибируется, готов, ошибка)
- Контекст предыдущих чанков всегда доступен

### 4. Прозрачность процесса
- Ясно видно, на какой стадии находится обработка
- SystemStatus показывает текущую активность системы
- Визуальная индикация транскрибируемого чанка

### 5. Чистота интерфейса
- Меньше элементов управления (убрана кнопка Live)
- Интерфейс не перегружен
- Фокус на контенте, а не на UI элементах

## Технические детали

### WebSocket события

**Используемые события:**
- `streaming_update` - обновление streaming транскрипции
- `chunk_created` - создание нового чанка
- `chunk_transcribed` - завершение транскрипции чанка
- `session_speakers` - обновление информации о спикерах
- `hybrid_processing_start` / `hybrid_processing_end` - гибридная обработка
- `llm_processing_start` / `llm_processing_end` - LLM обработка

### Анимации

**LiveChunksView:**
- `fadeIn` - появление нового чанка (0.3s)
- `transcribing-pulse` - пульсация транскрибируемого чанка (2s infinite)
- `highlight-flash` - мигание подтверждённого чанка (0.5s, 2 раза)
- `slideUp` - появление кнопки "Новых чанков" (0.3s)

**SystemStatus:**
- `pulse` - пульсация индикатора активности (2s infinite)

**RecordingOverlay:**
- `recordPulseRing` - пульсация индикатора записи (1.5s infinite)

### Цветовая схема

**Статусы чанков:**
- `completed` - #4caf50 (зелёный)
- `transcribing` - #ff9800 (оранжевый)
- `error` - #f44336 (красный)

**Стадии SystemStatus:**
- LLM - #a855f7 (фиолетовый)
- Гибрид - #f59e0b (янтарный)
- Спикеры - #8b5cf6 (фиолетовый)
- Транскрипция - #10b981 (зелёный)
- Стриминг - #3b82f6 (синий)
- Чанк - #6366f1 (индиго)
- Запись - #ef4444 (красный)
- Ожидание - #9ca3af (серый)

**Спикеры:**
- Циклический набор из 6 цветов: синий, зелёный, янтарный, красный, фиолетовый, розовый

## Обратная совместимость

### StreamingTranscription
- Полный режим (без `compact` prop) работает как раньше
- Все существующие использования компонента остаются рабочими
- Добавлен только новый режим, старый не изменён

### ChunksView
- Не изменён, работает как раньше
- Используется при просмотре записей
- LiveChunksView - отдельный компонент для live режима

## Возможные улучшения (будущие итерации)

### 1. Виртуализация списка
- При большом количестве чанков (>100) использовать `react-window`
- Улучшит производительность при длинных записях

### 2. Настройки отображения
- Опция скрывать/показывать streaming в чанке
- Настройка размера шрифта
- Выбор формата таймстемпов

### 3. Экспорт во время записи
- Кнопка "Скопировать всё" для текущей транскрипции
- Экспорт в TXT/JSON во время записи

### 4. Поиск по чанкам
- Поиск по тексту в реальном времени
- Фильтрация по спикерам

### 5. Редактирование на лету
- Исправление ошибок транскрипции во время записи
- Переименование спикеров в live режиме

## Тестирование

### Сценарии для проверки

1. **Базовая запись:**
   - Начать запись
   - Проверить появление SystemStatus
   - Проверить появление первого чанка
   - Проверить streaming текст в чанке

2. **Множественные чанки:**
   - Записать несколько чанков
   - Проверить автопрокрутку
   - Проверить highlight эффект
   - Проверить счётчик новых чанков

3. **Ручной скролл:**
   - Скроллить вверх во время записи
   - Проверить отключение автопрокрутки
   - Проверить появление кнопки "Новых чанков"
   - Проверить скролл вниз по клику

4. **Диаризация:**
   - Записать диалог с несколькими спикерами
   - Проверить цветовую кодировку
   - Проверить отображение имён спикеров

5. **Гибридная транскрипция:**
   - Включить hybrid mode
   - Проверить отображение стадии в SystemStatus
   - Проверить финальный результат в чанке

## Заключение

Рефакторинг успешно завершён. Новый интерфейс активного окна записи:
- ✅ Проще и понятнее
- ✅ Унифицирован с просмотром записей
- ✅ Более информативен
- ✅ Обратно совместим
- ✅ Готов к дальнейшему расширению

Все изменения локализованы в frontend компонентах, backend не затронут.
