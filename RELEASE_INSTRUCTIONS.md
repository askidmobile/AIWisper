# ✅ Релиз AIWisper v1.0.1 готов!

## Исправлена проблема с electron-updater

Проблема была решена: `electron-updater` перемещен из `devDependencies` в `dependencies` в `package.json`.

---

# Инструкция по созданию релиза AIWisper v1.0.1

## Файлы для релиза

Следующие файлы готовы для загрузки в GitHub Release:

### Обязательные файлы для автообновления:
1. **AIWisper-1.0.1-arm64.dmg** (263 MB) - Установщик для macOS (Apple Silicon)
2. **AIWisper-1.0.1-arm64.zip** (258 MB) - Архив для автообновления
3. **latest-mac.yml** - Метаданные для автообновления

### Дополнительные файлы:
4. **AIWisper-1.0.1-arm64.dmg.blockmap** - Для дифференциального обновления
5. **AIWisper-1.0.1-arm64.zip.blockmap** - Для дифференциального обновления

Все файлы находятся в: `/Users/askid/Projects/AIWisper/frontend/release/`

---

## Шаг 1: Инициализация Git (если еще не сделано)

```bash
cd /Users/askid/Projects/AIWisper

# Инициализация репозитория
git init

# Добавление файлов
git add .

# Первый коммит
git commit -m "Release v1.0.1

Исправления:
- Исправлено воспроизведение чанков - теперь играется только выбранный чанк
- Исправлен порядок диалога Вы/Собеседник через Voice Activity Detection
- Добавлены миллисекунды к таймстемпам для точной сортировки
- Улучшен UX: при начале записи очищается выбранная сессия
- Добавлено автообновление через electron-updater"

# Добавление удаленного репозитория (замените на ваш)
git remote add origin https://github.com/USERNAME/AIWisper.git

# Загрузка в GitHub
git push -u origin main
```

---

## Шаг 2: Создание Release в GitHub через веб-интерфейс

1. Перейдите в репозиторий: https://github.com/USERNAME/AIWisper
2. Нажмите "Releases" в правой панели
3. Нажмите "Create a new release"
4. Заполните форму:
   - **Tag version**: `v1.0.1`
   - **Release title**: `AIWisper v1.0.1`
   - **Description**:
   
   ```markdown
   ## 🎉 AIWisper v1.0.1
   
   ### 🐛 Исправления
   
   #### Воспроизведение чанков
   - ✅ Исправлено воспроизведение выбранного чанка - теперь играется только выбранный фрагмент, а не вся запись целиком
   - Добавлен новый API endpoint `/api/sessions/{sessionId}/chunk/{chunkIndex}.mp3`
   
   #### Порядок диалога
   - ✅ Исправлен неверный порядок реплик в диалоге (Вы/Собеседник)
   - Добавлен Voice Activity Detection для определения реального начала речи в каждом канале
   - Таймстемпы теперь показывают точное время с десятыми долями секунды (MM:SS.d)
   
   #### UX улучшения
   - ✅ При нажатии "Запись" автоматически очищается выбранная сессия
   - Теперь сразу видна транскрибация новой записи в реальном времени
   
   ### ✨ Новые возможности
   
   - 🔄 **Автообновление**: Приложение теперь автоматически проверяет и устанавливает обновления
   - При запуске приложение проверит наличие новой версии
   - Обновления загружаются в фоне и устанавливаются при следующем запуске
   
   ### 📦 Установка
   
   **macOS (Apple Silicon):**
   1. Скачайте `AIWisper-1.0.1-arm64.dmg`
   2. Откройте DMG файл
   3. Перетащите AIWisper в папку Applications
   4. При первом запуске разрешите доступ к микрофону и записи экрана
   
   ### ⚠️ Важно для разделения по каналам
   
   Чтобы получить раздельную транскрибацию "Вы" и "Собеседник":
   1. Включите "System Audio" в настройках
   2. Включите "Voice Isolation" (требует macOS 15+)
   3. Сделайте новую запись
   
   Старые записи были сделаны в моно режиме и не могут быть разделены постфактум.
   
   ### 📝 Changelog
   
   **Backend:**
   - Добавлен Voice Activity Detection (VAD) в `session/vad.go`
   - Применяется offset к таймстемпам для правильной сортировки диалога
   - Добавлен API endpoint для извлечения конкретных чанков из MP3
   
   **Frontend:**
   - Улучшено отображение времени с миллисекундами
   - Добавлена очистка выбранной сессии при начале новой записи
   - Добавлен индикатор режима стерео/моно для сессий
   - Интегрирован electron-updater для автообновлений
   
   ---
   
   **Полный список изменений**: v1.0.0...v1.0.1
   ```

5. **Загрузите файлы** (drag & drop в секцию "Attach binaries"):
   - `AIWisper-1.0.1-arm64.dmg`
   - `AIWisper-1.0.1-arm64.zip`
   - `latest-mac.yml`
   - `AIWisper-1.0.1-arm64.dmg.blockmap` (опционально)
   - `AIWisper-1.0.1-arm64.zip.blockmap` (опционально)

6. Нажмите "Publish release"

---

## Шаг 3: Создание Release через GitHub CLI (альтернатива)

```bash
cd /Users/askid/Projects/AIWisper

# Установите GitHub CLI если еще не установлен
# brew install gh

# Авторизация
gh auth login

# Создание релиза
gh release create v1.0.1 \
  --title "AIWisper v1.0.1" \
  --notes-file RELEASE_NOTES.md \
  frontend/release/AIWisper-1.0.1-arm64.dmg \
  frontend/release/AIWisper-1.0.1-arm64.zip \
  frontend/release/latest-mac.yml \
  frontend/release/AIWisper-1.0.1-arm64.dmg.blockmap \
  frontend/release/AIWisper-1.0.1-arm64.zip.blockmap
```

---

## Проверка автообновления

После создания релиза:

1. Установите старую версию (1.0.0) на другом Mac
2. Запустите приложение
3. Через ~3 секунды после запуска проверьте логи - должно появиться:
   ```
   Checking for updates...
   Update available: 1.0.1
   ```
4. Обновление загрузится автоматически
5. Появится диалог с предложением перезапустить приложение
6. После перезапуска будет установлена версия 1.0.1

---

## Проверка настроек репозитория

Убедитесь что в `frontend/package.json` указаны правильные настройки:

```json
"publish": [
  {
    "provider": "github",
    "owner": "YOUR_GITHUB_USERNAME",
    "repo": "AIWisper"
  }
]
```

Замените `YOUR_GITHUB_USERNAME` на ваш GitHub username или организацию.

---

## Troubleshooting

### Автообновление не работает

1. Проверьте что в релизе загружены все файлы:
   - `AIWisper-1.0.1-arm64.zip` (обязательно)
   - `latest-mac.yml` (обязательно)

2. Проверьте логи Electron:
   ```bash
   # В dev режиме
   tail -f ~/Library/Logs/AIWisper/main.log
   ```

3. Проверьте что в `package.json` правильно указан репозиторий

### DMG не открывается на другом Mac

1. Подпишите приложение Developer ID сертификатом:
   ```bash
   codesign --deep --force --verify --verbose \
     --sign "Developer ID Application: YOUR NAME (TEAM_ID)" \
     "AIWisper.app"
   ```

2. Нотаризуйте приложение:
   ```bash
   xcrun notarytool submit AIWisper-1.0.1-arm64.dmg \
     --apple-id "your@email.com" \
     --team-id "TEAM_ID" \
     --password "app-specific-password"
   ```

---

## Следующие шаги

После успешного релиза:

1. ✅ Проверьте что автообновление работает
2. ✅ Обновите документацию в README.md
3. ✅ Создайте changelog для следующей версии
4. ✅ Закройте соответствующие issues в GitHub

---

**Дата создания**: 2025-12-01  
**Версия**: 1.0.1  
**Платформа**: macOS (Apple Silicon)
